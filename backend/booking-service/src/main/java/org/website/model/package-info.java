/**
 * Local persistence model for booking-service.
 *
 * <p>These classes are service-owned and must never be shared across service
 * boundaries. Inter-service communication is DTO-based only.</p>
 *
 * <p>Migration status: legacy duplicate models are being phased out in favor of
 * strict service ownership boundaries and DTO-only contracts.</p>
 */
package org.website.model;
